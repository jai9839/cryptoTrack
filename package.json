{
  "name": "cryptotrack",
  "version": "1.0.0",
  "private": true,
  "description": "CryptoTrack MERN app — build & run",
  "scripts": {
    "install:all": "npm install --prefix Server && npm install --prefix Client",
    "build": "npm run build --prefix Client",
    "start:api": "npm start --prefix Server",
    "start:app": "cross-env SERVE_CLIENT=true npm start --prefix Server",
    "make": "npm run build && npm run start:app",
    "dev": "concurrently \"npm run start:api\" \"npm run dev --prefix Client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
